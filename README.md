# wp-theme-build

WordPress 테마 빌드 작업 저장소.

## 다른 맥에서 작업 시작하기

```bash
git clone <이-저장소-URL> wp-theme-build
cd wp-theme-build
```

## 일상 워크플로우

```bash
# 작업 시작 전 최신 내용 받기
git pull

# 작업 후
git add -A
git commit -m "설명"
git push
```
